#if !defined(PRAYER_types)
#define PRAYER_types
#if prayer_major_version >= 0 && prayer_middle_version >= 0 && prayer_minor_version >= 1

namespace prayer
{
    using u8 = unsigned char;

    using s8 = char;

    using u16 = unsigned short;
    
    using s16 = short;

    using f16 = half;

    using u32 = unsigned int;

    using s32 = int;

    using f32 = float;

    using u64 = unsigned long;

    using s64 = long;
}

#endif
#endif